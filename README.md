# streamlit-Ml-dashb
Developed an interactive ML dashboard using Streamlit that displays real-time model predictions. Integrated trained models (.pkl files), applied data preprocessing with StandardScaler, and enabled both live user input prediction and batch CSV prediction.
